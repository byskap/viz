# Viz

IMU data visualization for the tracking project using Rerun.

## Quick Start (with uv)

If you have `uv` installed, you can run the project directly:

```bash
uv run viz
```

## Installation

### Using uv
```bash
uv sync
```