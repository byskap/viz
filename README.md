# Snow Viz

IMU data visualization for the Snow project using Rerun.

## Quick Start (with uv)

If you have `uv` installed, you can run the project directly:

```bash
uv run snow-viz
```

## Installation

### Using uv
```bash
uv sync
```