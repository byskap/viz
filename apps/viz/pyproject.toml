[project]
name = "viz"
version = "0.1.0"
dependencies = [
    "rerun-sdk==0.30.2",
    "serial-parser"
]

[project.scripts]
viz = "viz.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/viz"]
