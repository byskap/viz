[project]
name = "logger"
version = "0.1.0"
dependencies = [
    "mcap>=1.1.1",
    "serial-parser"
]

[project.scripts]
logger = "logger.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/logger"]
