[project]
name = "viz-workspace"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "serial-parser"
]

[tool.uv.workspace]
members = ["packages/*", "apps/*"]

[tool.uv.sources]
serial-parser = { workspace = true }
